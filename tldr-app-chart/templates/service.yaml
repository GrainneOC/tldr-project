apiVersion: v1
kind: Service
metadata:
  name: {{ include "tldr-app-chart.fullname" . }}
  labels:
    {{- include "tldr-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tldr-app-chart.selectorLabels" . | nindent 4 }}
